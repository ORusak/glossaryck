* Работает по верх TCP IP
* Вначале браузер запрашивает по HTTP подддерживает ли сервер, потом переходит на протокол
* WS, WSS
* Один метод send
* 4 события
    * onmessage
    * onopen
    * onerror
    * onclose
* 